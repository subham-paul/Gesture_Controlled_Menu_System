# config.py
FRAME_W = 1280
FRAME_H = 720

# Cursor smoothing: 0.0 = no smoothing, 1.0 = full smoothing (very slow)
CURSOR_SMOOTHING = 0.35

# Pinch threshold in PIXELS (thumb tip ↔ index tip). Tune if needed.
# Typical working: 32-50 depending on camera FOV. Start with 40.
PINCH_THRESHOLD_PIXELS = 40

# Feedback duration for messages (seconds)
FEEDBACK_DURATION = 1.2
